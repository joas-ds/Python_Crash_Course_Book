'''
Using the input() function
Using while
'''

# #Writing long input using +=
# prompt = "If you tell us who you are, we can personalize the messages you see."
# prompt += "\nWhat is your first name?\n"
# #Above we assing one value to the var prompt and then anoter using +=, wich will
# #add the other line to the variable
# name = input(prompt)
# print(f"Hello, {name.title()}")
#
# #The Modulo (%) Operator:
# #It divides a number by anoter and returns the REMAINDER
# #When the number is divisible by the other number, it returns
# #0, we can use that to tell if the number is even or odd.
# #e.g:
# num = int(input("Digite um número e eu vou dizer se ele é par ou ímpar: "))
# if num % 2 == 0:
#     print(f"O número {num} é par.")
# else:
#     print(f"O número {num} é ímpar.")

